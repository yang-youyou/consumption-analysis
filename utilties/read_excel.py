import numpy as np
import pandas as pd

class Excel:
    def __init__(self, old_file):
        self.old_file = old_file
    def clear_excel(self):
           print("check")
    def rsa_mail(self，str，key): #加密
        return rsa(str,key)
